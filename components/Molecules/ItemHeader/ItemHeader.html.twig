{% set href = href|default('') %}
{% set classes = classes|default('') %}
{% set customText = customText|default('') %}
{% set sub = sub|default(null) %}

<li class="flex">
	<a href='{{ href }}' class='ItemHeader {{ classes }}'>
		{{ customText }}
	</a>
	{% if sub %}
		<button type="button" data-action="header#displaySubMenu" data-header-item-param="{{sub}}">{{ ux_icon("arrow-right") }}</button>
		<ul data-menu-sub="{{sub}}" data-menu-previous="0" data-header-target="sub">
			<li>
				<a href="#0">Titre sous menu
				</a>
				<button type="button" data-action="header#displaySubMenu" data-header-item-param="{{sub}}_5">{{ ux_icon("arrow-right") }}</button>
				<ul data-menu-sub="{{sub}}_5" data-menu-previous="{{sub}}" data-header-target="sub">
					<li>
						<a href="#0">Titre sous sous menu</a>
					</li>
				</ul>
			</li>
			<li>
				<a href="#0">Titre sous menu</a>
			</li>
			<li>
				<a href="#0">Titre sous menu</a>
			</li>
			<li>
				<a href="#0">Titre sous menu</a>
			</li>
		</ul>
	{% endif %}

</li>
