<div class="RootToast {{attribute.render('class')}}" {{attribute}}>
	{% for type, messages in app.session.flashbag.all() %}
		{% for message in messages %}
            {{ component('Flexy:SnackBar', {
                text: message,
                variant: type,
                withIcon: true,
                size: 'full',
                button: true
                })
            }}
		{% endfor %}
	{% endfor %}
</div>
