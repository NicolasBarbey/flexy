<{{ tag }} class="Checkbox {{ checked ? 'checked' : '' }}" {{ attributes.without('class') }}>
    <div class="checkmark">
        {{ ux_icon('checkbox') }}
    </div>
    <span class="paragraph-5">{{ text|raw }}</span>
</{{ tag }}>
